<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <% if (result.name) { %>
        <h3>argument passer dans le req</h3>
        <code><%= JSON.stringify(result)%></code>
    <% } else { %>
        <h3>pas d'argument passer</h3>
    <% } %>
</body>
</html>